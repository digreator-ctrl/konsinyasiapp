{
  "name": "@konsinyasi/api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:migrate:local": "wrangler d1 migrations apply konsinyasi-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply konsinyasi-db --remote"
  },
  "dependencies": {
    "@konsinyasi/shared": "workspace:*",
    "hono": "^4.7.0",
    "drizzle-orm": "^0.39.0",
    "zod": "^3.24.0",
    "nanoid": "^5.1.0",
    "better-auth": "^1.2.0"
  },
  "devDependencies": {
    "@konsinyasi/tsconfig": "workspace:*",
    "@cloudflare/workers-types": "^4.20250109.0",
    "drizzle-kit": "^0.30.0",
    "typescript": "^5.8.0",
    "wrangler": "^4.0.0"
  }
}
